apiVersion: apps/v1
kind: Deployment
metadata:
  name: guest-service
  namespace: hotel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: guest-service
  template:
    metadata:
      labels:
        app: guest-service
    spec:
      containers:
        - name: guest-service
          image: hotel-guest-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8083
          env:
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                configMapKeyRef:
                  name: hotel-config
                  key: SPRING_DATASOURCE_URL
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: hotel-secrets
                  key: postgres-username
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: hotel-secrets
                  key: postgres-password