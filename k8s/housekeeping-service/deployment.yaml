apiVersion: apps/v1
kind: Deployment
metadata:
  name: housekeeping-service
  namespace: hotel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: housekeeping-service
  template:
    metadata:
      labels:
        app: housekeeping-service
    spec:
      containers:
        - name: housekeeping-service
          image: abubakarmeigag/hotel-housekeeping-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8084
          env:
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                configMapKeyRef:
                  name: hotel-config
                  key: SPRING_DATASOURCE_URL
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: hotel-secrets
                  key: postgres-username
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: hotel-secrets
                  key: postgres-password
            - name: SPRING_RABBITMQ_HOST
              valueFrom:
                configMapKeyRef:
                  name: hotel-config
                  key: SPRING_RABBITMQ_HOST